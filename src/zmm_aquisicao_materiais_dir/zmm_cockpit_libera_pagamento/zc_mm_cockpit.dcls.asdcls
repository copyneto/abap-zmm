@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZC_MM_COCKPIT {
    grant
        select
            on
                ZC_MM_COCKPIT
                    where
                        inheriting conditions from entity ZI_MM_COCKPIT;
                        
}