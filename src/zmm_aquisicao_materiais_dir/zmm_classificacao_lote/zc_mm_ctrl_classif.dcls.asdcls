@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZC_MM_CTRL_CLASSIF {
    grant
        select
            on
                ZC_MM_CTRL_CLASSIF
                    where
                        inheriting conditions from entity ZI_MM_CTRL_CLASSIF;
                        
}