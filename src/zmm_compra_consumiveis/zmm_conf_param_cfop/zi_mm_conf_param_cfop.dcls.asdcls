@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZI_MM_CONF_PARAM_CFOP {
    grant 
        select
            on
                ZI_MM_CONF_PARAM_CFOP
                    where
                        (Centro) = aspect pfcg_auth(ZMMWERKS, WERKS, ACTVT = '03');                                                
}