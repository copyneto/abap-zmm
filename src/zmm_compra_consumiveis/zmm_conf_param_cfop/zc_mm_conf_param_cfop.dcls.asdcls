@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZC_MM_CONF_PARAM_CFOP {
    grant
        select
            on
                ZC_MM_CONF_PARAM_CFOP
                    where
                        inheriting conditions from entity ZI_MM_CONF_PARAM_CFOP;
                        
}