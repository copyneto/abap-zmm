projection;

define behavior for ZC_mm_RESERVAS_PENDENTES_APP //alias <alias_name>
{
  use update;
  use delete;

  use action fragmentCheckBox;
  use action fragmentUsedStock;

  use action utilizarQtde;
  use action criarNFE;
  use action qtdeTransferida;
}