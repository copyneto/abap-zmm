@EndUserText.label: 'CDS de autorização'
@MappingRole: true
define role ZC_VISAO_REMESSA_RETORNO {
    grant
        select
            on
                ZC_VISAO_REMESSA_RETORNO
                    where
                        inheriting conditions from entity ZI_VISAO_REMESSA_RETORNO;
                        
}