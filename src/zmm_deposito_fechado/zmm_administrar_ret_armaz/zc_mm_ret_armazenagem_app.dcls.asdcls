@EndUserText.label: 'CDS de autorização'
@MappingRole: true
define role ZC_MM_RET_ARMAZENAGEM_APP {
    grant
        select
            on
                ZC_MM_RET_ARMAZENAGEM_APP
                    where
                        inheriting conditions from entity ZI_MM_RET_ARMAZENAGEM_APP;
                        
}