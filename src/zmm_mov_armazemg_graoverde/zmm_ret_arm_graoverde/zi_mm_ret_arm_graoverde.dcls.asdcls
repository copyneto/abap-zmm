@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZI_MM_RET_ARM_GRAOVERDE {
    grant
        select
            on
                ZI_MM_RET_ARM_GRAOVERDE
                    where
                        ( WerksCode ) = aspect pfcg_auth( ZMMWERKS, WERKS, ACTVT = '03' );
                        
                        
}